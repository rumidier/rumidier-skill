Thaillum 작업중 해쉬에해쉬를 만들어야 하는 경우가 생겼다
하지만 현재 내가 아는 지식으로는 해쉬를 한방에 만들지 않는한
어떻게 만들어주어야 할지 감을 못잡고 있었고 이에 따른 질문을 하였다

VAR {
	ID => {
		Name   => 조한영,
		PIC    => 2001-02-02,
		index  => 2001-03-02,
		follow => 2002-03-20,
	}
}

한번에 만들어 주는 것이 아닌 ID에 해당 하는 키와밸류 값들을
for 문을 통해 순서적으로 하는것을 해보겠다

하는 방법으로는 

for my $col ( $cols ) {
	$hash{ID}{$col} = $rows[$count];
}
해주면 된다
